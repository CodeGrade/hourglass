<%= react_component('ShowExam', {
  user: current_user,
  preview: preview,
  final: @final,
  registration: {
    id: @registration.id,
  },
  exam: {
    id: @exam.id,
    name: @exam.name,
  }
}) %>
