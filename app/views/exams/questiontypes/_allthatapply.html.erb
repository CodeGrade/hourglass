<!--TODO -->
<%= render "exams/prompt", item: item %>
<% qtitle = :need_some_unique_name #TODO %>
<%= bootstrap_form_tag do |f| %>
  <%= f.form_group qtitle, label: { text: "Select all that apply." } do %>
    <% num = 0 # TODO: are these always in order? %>
    <% item["options"].each do |option| %>
      <%= f.check_box "#{qtitle}_#{num}", label: option.keys[0] %>
      <% num += 1 %>
    <% end %>
  <% end %>
<% end %>
