<%= @form.form_group unique_label, label: { text: "Select one of the following responses." } do %>
  <% item["options"].each_with_index do |option, index| %>
    <%= @form.radio_button unique_label, index, label: option.html_safe %>
  <% end %>
<% end %>
