<% @keepnavbar = true %>
<%= react_component('professor/exams/new', {
}) %>

rails form:

<%= bootstrap_form_for Exam.new, url: professor_exams_path do |f| %>
  <%= f.text_field :name %>
  <%= f.check_box :enabled %>
  <%= f.select :course_id, @courses %>
  <%= f.file_field :file %>
  <%= f.submit %>
<% end %>
